# 统一使用 LF 换行符。
# 在 Windows 上如果不指定，检出时会变成 CRLF，跨平台协作时会和 CI（Linux）
# 产生整文件的换行符 diff；对 Gemfile.lock 这类文件尤其不友好。
* text=auto eol=lf

# 二进制文件不做行尾转换
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary
*.ico  binary
*.woff binary
*.woff2 binary
*.ttf  binary
*.pdf  binary

# 脚本类强制 LF
*.sh text eol=lf
